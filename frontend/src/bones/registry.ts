import { registerBones } from 'boneyard-js/react';

// Placeholder registry for initial integration.
// `npm run boneyard:build` will generate real `*.bones.json` files and can
// overwrite runtime registration with captured skeleton descriptors.
registerBones({});
